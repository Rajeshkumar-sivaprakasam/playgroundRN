module.exports = {
  arrowParens: 'avoid', // No parentheses for single-arg arrow functions
  bracketSpacing: true, // Adds spaces inside object braces { foo: bar }
  singleQuote: true, // Prefer single quotes instead of double quotes
  trailingComma: 'all', // Adds trailing commas in objects, arrays, etc.
  semi: false, // Removes semicolons at the end of lines
  tabWidth: 2, // Use 2 spaces for indentation
  useTabs: false, // Use spaces instead of tabs
  printWidth: 100, // Wrap lines that exceed 100 characters
  endOfLine: 'auto', // Adjusts to system's end-of-line format
  jsxSingleQuote: true, // Use single quotes in JSX
  bracketSameLine: false, // Puts closing `>` of JSX tags on a new line
}
